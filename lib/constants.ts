/**
 * 🏷️ TALLUP — App Identity
 *
 * Change these values to rebrand. They propagate across landing, auth,
 * onboarding, and profile screens.
 */

/** Display name shown in headers, onboarding, and the landing page. */
export const APP_NAME = 'TallUp'

/** Tagline */
export const APP_TAGLINE = 'Grow with intention.'

/** Short description used in marketing / onboarding contexts. */
export const APP_DESCRIPTION =
  'Track every centimeter. Optimize sleep, nutrition, and exercise for natural growth.'

/** URL scheme matching expo.scheme in app.json — used for OAuth deep link callbacks. */
export const APP_SCHEME = 'tallup'

/** Support email shown on the Support screen. */
export const APP_SUPPORT_EMAIL = 'support@tallup.app'

/** Documentation URL shown on the Support screen. */
export const APP_DOCS_URL = 'https://tallup.app/docs'
